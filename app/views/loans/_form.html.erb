<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount_in_cents %>
    <%= form.number_field :amount_in_cents, id: :loan_amount_in_cents %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :loan_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
